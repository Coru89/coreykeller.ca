{% from "macros/macros.njk" import button_link %}

<section class="work-panel{% if props.isBottom === true %} work-panel--bottom{% endif %}">
  <div class="work-panel__divider">
    <svg data-name="Layer 1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 120" preserveAspectRatio="none">
      <path d="M1200 120L0 16.48 0 0 1200 0 1200 120z" class="shape-fill"></path>
    </svg>
  </div>
  <div class="work-panel__inner{% if props.imagePosition === 'right' %} work-panel__inner--reverse{% endif %}" style="background-color: {{colorHexCard}};">
    <div class="work-panel__row">
        <h2>{{ props.title }}</h2>
        <h3 class="section__heading">{{ props.titleSecondary }}</h3>
    </div> 
    <div class="work-panel__row">
      {% for item in props.items %} 
        <div class="work-panel__item">
          <div class="work-panel__image{% if item.imagePosition === 'right' %} work-panel__image--reverse{% endif %}">
            {% if item.imageURL.length %}
              {% image item.imageURL, item.imageAlt, null, "lazy", "100vw", ['300', '400', '576', '600', '730', '900', '1046', '1100', '1500', '2000', '3000'] %}
            {% endif %}
          </div>
          <div class="work-panel__text{% if item.imagePosition === 'right' %} work-panel__text--reverse{% endif %}">
            <h2 class="work-panel__heading">Featured Work</h2>
            <h3 class="work-panel__text-title">{{ item.title }}</h3>
            <div class="work-panel__text-summary">{{item.summary | safe }}</div>
              {% if item.buttonText.length and item.buttonPath.length %}
                {{ arrow_link_large(item.buttonText, item.buttonPath) }}
              {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

  <style>
    .work-panel__divider {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      overflow: hidden;
      line-height: 0;
    }

    .work-panel__divider svg {
      position: relative;
      display: block;
      width: calc(100% + 1.3px);
      height: 120px;
      transform: rotateY(180deg);
    }

    .work-panel__divider .shape-fill {
      fill: #B4CECF;
      fill: #ECD8D5;
    }
  </style>


